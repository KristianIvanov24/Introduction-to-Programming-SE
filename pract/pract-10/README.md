# Указатели. Символни низове.

### В час

1. Напишете функция `to_string`, която приема число и го превръща в символен низ.

2. Напишете функция, която приема два символни низа и проверява дали първия се среща във втория.

### Задачи

1. Напишете функция, която приема символен низ и превръща всички негови главни букви в малки и всички малки в главни.

<details> <summary>Примерен вход и изход</summary>

Вход: 
```
"This, HERe, is my BeLoVED sTRING!"
```

Изход: 
```
"tHIS, herE, IS MY bElOved String!"
```
</details>


2. Напишете функция, със сигнатура `long convertNumber(const char* numStr)`, която връща като резултат сумата от ASCII кодовете на цифрите на числото, представено с арабски цифри чрез символния низ num. Ако num не съдържа коректно число (т.е. има символи, които не принадлежат на множеството от арабски цифри от 0 до 9), функцията да връща -1.

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"123" 
```
Изход:
```
150 // ascii code of '1' is 49, '2' - 50, '3' - 51
```
---
Вход:
```
"a435" 
```
Изход:
```
-1
```
</details>

3. Напишете функция, която приема като параметър низ и проверява дали той е поредица от редуващи се малки и големи букви от латинската азбука.

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"StRiNgIsTrUe" 
```
Изход:
```
1
```
---
Вход:
```
"sTrInGiStRuE" 
```
Изход:
```
1
```
---
Вход:
```
"sTrInG iS fAlSe" 
```
Изход:
```
0
```
---
Вход:
```
"sTrInG_iS$fAl$e" 
```
Изход:
```
0
```
---
Вход:
```
"wazaaa" 
```
Изход:
```
0
```

</details>

4. Напишете функция, която приема символен низ и връща броя на думите в него. (Думите са разделени с произволен брой интервали, табулации и/или знаци)

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"! This         string has 7 words.. Wohoo  !cpp" 
```
Изход:
```
7
```
---
Вход:
```
"a!(#*&b@$)%@*a)#!z" 
```
Изход:
```
4
```
</details>

5. Напишете функция, която приема символен низ `text`, символ `toInsert` и индекс `i` и вмъква `toInsert` в `text` на позиция `i`.

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"Hello, orld!"
w
7
```
Изход:
```
"Hello, world!"
```
</details>

6. Напишете функция, която приема символен низ `text`, символ `toDelete` и премахва всяко срещане на `toDelete` в `text`.

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"Hello, world!"
'o'
```
Изход:
```
"Hell, wrld!"
```

</details>

7. Напишете функция, която приема два символни низа `text`, `toInsert` и индекс `i` и вмъква `toInsert` в `text` на позиция `i`.

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"Hello, orld!"
"beautiful w"
7
```
Изход:
```
"Hello, beautiful world!"
```
</details>

8. Напишете функция, която приема два символни низа `text` и `toDelete` и премахва всяко срещане на `toDelete` в `text`.

<details> <summary>Примерен вход и изход</summary>

Вход:
```
"Hello gorgeous - FMI!"
"o gorgeous"
```
Изход:
```
"Hell - FMI!"
```
</details>

9.  Напишете функция, която приема два символни низа и символен низ `result`. Функцията да обработва result така, че да предтсавлява символен низ, който съдържа разликите между подадените.

<details> <summary>Примерен вход и изход</summary>

Вход: 
```
abcde 
acdefgh
```
Изход: 
```
bfgh
```
</details>

10. Напишете функция, която приема символен низ и го обработва така, че всяка нова дума да започва с главна буква и всяка останала буква от думата да е малка.

<details> <summary>Примерен вход и изход</summary>

Вход: 
```
"This will be, ,an every WORD CaPiTALIzED sentence!!!"
```
Изход: 
```
"This Will Be, ,An Every Word Capitalized Sentence!!!"
```
</details>