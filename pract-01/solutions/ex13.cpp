// 13. Напишете програма, при която се въвеждат две реални числа (две страни на правоъгълник). Програмата да изчисли и 
// отпечата периметъра и лицето на правоъгълника. Програмата да отпечатва дали правоъгълника е и квадрат.
#include <iostream>

int main () {
    double a, b; std::cin>>a>>b;
    std::cout<<"Perimeter: "<<(2 * (a + b))<<"\nArea: "<<(a * b)<<"\nIs Square: "<<(a == b)<<std::endl;

    return 0;
}