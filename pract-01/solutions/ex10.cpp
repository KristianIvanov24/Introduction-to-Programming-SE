// 10. Въвеждат се 4 цели числа. Да се отпечата 1, ако числата образуват аритметична прогресия в реда, в който 
// са въведени, и 0, ако съответно не образуват.
#include <iostream>

int main () {
    int a, b, c, d; std::cin>>a>>b>>c>>d;
    std::cout<<(abs(a - b) == abs(b - c) && abs(b - c) == abs(d - c))<<std::endl;

    return 0;
}